(()=>{'use strict';const detail=document.querySelector('#architecture-detail');const copy={frontend:['FRONTEND','Server-rendered pages, route navigation, progressive enhancement, keyboard access and reduced-motion behavior.'],content:['CONTENT','Projects, notes, profile and engineering records are kept in structured sources instead of duplicated page copy.'],backend:['BACKEND','PHP route handling, repositories, validation, security helpers and server-side rendering.'],data:['DATA','PostgreSQL-backed records and numbered migrations provide the persistent content layer.']};document.querySelectorAll('[data-arch-layer]').forEach(button=>button.addEventListener('click',()=>{const item=copy[button.dataset.archLayer];if(!item||!detail)return;detail.innerHTML='<span>'+item[0]+'</span><p>'+item[1]+'</p>'; }));})();